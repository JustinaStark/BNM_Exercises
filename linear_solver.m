function [x] = linear_solver(A, b)
% LINEAR_SOLVER Direct solver for Ax = b using Gaussian elimination and partial
% pivoting
%   x: Unknown vector
%   A: Regular, square matrix
%   b: RHS vector of Ax = b




x = 0;
end

